1. npm i
2. npm i @angular/material
3. npm i encryptjs

4. json-server --watch db.json ( also make sure you have the apiURL set => bellow the string, commented if not already in )

bear in mind, as of 2025.03.10, we're starting with the integration of the backend, db.json is somewhat obsolete now(purpose was to see the data).
    also, data is no longer visible as now one can only see their own 'owned' tasklists and tasks.
